Dev only
New versions must be distinct from each other - e.g v1, v2, v3.
Export blender models and Import blender models as obj. files (Waveform)
to update new versions go to the Add File on the code tab on GitHub (left of Issues) and upload a file.
Add more rules here if need be

